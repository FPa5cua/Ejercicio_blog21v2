<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Artículo</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <%-include("partials/nav")%>

    <!-- Hero -->
    <section class="d-flex hero-article justify-content-center align-items-center">
      <div class="container">
        <h1 class="display-5 text-white fw-bold">
          Man must explore, and this is exploration at its greatest
        </h1>
        <p class="h4 text-center text-white">Creado por <%= article.user.firstname %> <%= article.user.lastname %> el <%= format(article.createdAt, 'dd MM yyyy') %></p>
      </div>
    </section>
   

    <!-- FIN Hero -->

    <section class="container w-50 my-5 fw-semi-bold">
      <div>
      
        <p class="mt-2">
          <%= article.content %>
        </p>
        <p class="mt-2">
          
        </p>
      </div>
      <hr />

      <form action="/comentarios/<%= article.id %>" method="post" class="mb-5">
        <label for="UserComment" class="fw-bold h2">Agregue su comentario</label>
        <input
          class="form-control mb-3"
          type="text"
          placeholder="Ingrese su Nombre"
          name="firstname"
          id="firstname"
        />
        <textarea
          class="form-control mb-3"
          name="commentCreated"
          id="commentCreated"
          cols="30"
          rows="8"
        ></textarea>
        <div class="d-grid gap-2">
          <button type="submit" class="btn btn-primary btn-block fw-bold">Enviar</button>
        </div>
        
      </form>
      <h3 class="fw-bold mb-3">Comentarios</h4>

      <div class="mb-3 pb-3 border-bottom">
        <% for (const comment of article.comments) { %>
           
      

            <p><%= comment.content %></p>
            
           <!-- No funciona el input de nombre D: -->
          

           

        <%}%>
    
        

      </div>

    </section>

    <%-include("partials/footer")%>

    <script src="https://kit.fontawesome.com/fefc105dd4.js" crossorigin="anonymous"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
